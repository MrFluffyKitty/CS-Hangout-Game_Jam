import pygame
import os

# Loads background image and sets it to screen dimensions
# BACKGROUND = pygame.transform.scale(pygame.image.load(os.path.join(
# 'game_jam', 'Assets', 'Background', 'Galaxy_bg', 'Purple_Nebula', 'PN1.png')), (700, 700)).convert()
